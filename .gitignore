node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
coverage/

# Local dev API pulls - separate timestamped files per call, never
# committed (RentCast free-tier calls are precious). The CI-persisted
# equivalent lives in the tracked ./data/ directory at the repo root.
backend/data/
